# GOLDEN reference embeddings (§1) — line format: label<TAB>f0,f1,...  where label is
# "<input-id>|<pooling>" (matching the `correctness` crate's emit output).
#
# These are produced OFFLINE, once, by the upstream official `llama-embedding` binary
# built from the pinned llama.cpp commit with GENERIC flags — an implementation
# independent of the tuned archives this repo ships, so the parity check is not circular.
# Regenerate with scripts/gen-golden.sh whenever the reference model (reference-model.env)
# or the pinned llama.cpp commit (scripts/config.env EXPECTED_LLAMA_CPP_COMMIT) changes.
#
# Provenance (fill in when generating):
#   llama.cpp commit : 9e3b928fd8c9d14dbf15a8768b9fdd7e5c721d66  (describe: b9553)
#   reference model  : bge-small-en-v1.5-q8_0.gguf  sha256 <fill-in>
#   build flags      : -O3 -march=armv8-a  (generic; no dotprod/fp16/mtune)
#   tool             : llama-embedding --pooling {mean,last} --embd-normalize 2
#
# UNTIL this file has data rows, scripts/correctness.sh treats §1 golden parity as
# "not_generated": it logs a warning and records it in build-info.json, while the §2
# tuned-vs-generic and §4 self-consistency checks remain HARD release gates. Run
# scripts/gen-golden.sh and commit the result to activate the golden gate.
